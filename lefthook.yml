pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,json}"
      run: pnpm run lint
    typecheck:
      run: pnpm run typecheck
    test:
      run: pnpm test
    format:
      glob: "*.ts"
      run: pnpm run format:check
